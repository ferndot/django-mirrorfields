============
Installation
============

At the command line::

    $ easy_install django-mirrorfields

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mirrorfields
    $ pip install django-mirrorfields
